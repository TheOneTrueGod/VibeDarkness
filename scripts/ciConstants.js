/** @typedef {import('./ciTypes.js').CiResults} CiResults */

export const CI_INTERVAL_MS = 30 * 60 * 1000;

/** Relative to project root; matches PHP handler and frontend API. */
export const CI_RESULTS_RELATIVE_PATH = 'storage/ci_results.json';

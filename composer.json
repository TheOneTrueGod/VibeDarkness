{
    "name": "multiplayer/lobby",
    "description": "Multiplayer game lobby system with HTTP polling",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "autoload": {
        "psr-4": {
            "App\\": "backend/"
        }
    }
}

import type { WorldModifierDef } from '../types';

/** Built-in world modifiers always active for every mission. Empty in v1 — see Decision A in architecture docs. */
export const BUILTIN_WORLD_MODIFIERS: WorldModifierDef[] = [];

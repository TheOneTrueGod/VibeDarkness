/**
 * Whether the local player has no loadout choices to make on character select.
 * When true, the loadout selector shows its empty state (campfire).
 *
 * Stub: always true until loadout options exist.
 */
export function playerDoesNotNeedLoadoutSelection(): boolean {
    return true;
}

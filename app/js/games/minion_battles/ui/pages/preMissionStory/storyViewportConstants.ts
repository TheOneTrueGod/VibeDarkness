/** Story scroll region height below this → use laptop (side-by-side) dialogue layout */
export const STORY_VIEWPORT_CONTAINER_LAPTOP_MAX_PX = 500;

/** Window inner height below this → laptop layout (fallback when container is ambiguous) */
export const STORY_VIEWPORT_WINDOW_LAPTOP_MAX_PX = 750;

/**
 * Laptop sidebar portrait square (px). CharacterPortrait presets are 96 / 180 / 240; 200 is between medium and large.
 */
export const STORY_LAPTOP_PORTRAIT_PX = 200;
